@model IEnumerable<PM.Models.Supplier>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenNXS)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenNXS)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaNXS }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaNXS }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaNXS })
        </td>
    </tr>
}

</table>
